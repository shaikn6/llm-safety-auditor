fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1

# LLM mock (optional: real OpenAI calls)
openai==1.30.1

# Semantic detection (optional: falls back to TF-IDF if not installed)
sentence-transformers==3.0.1

# PDF generation
reportlab==4.2.0

# Dashboard
streamlit==1.35.0
plotly==5.22.0
pandas==2.2.2

# Visualization
matplotlib==3.9.0
numpy==1.26.4

# Testing
pytest==8.2.1
pytest-cov==5.0.0
httpx==0.27.0

# Dev
python-dotenv==1.0.1

# V2: attack replay SQLite persistence (stdlib; listed for clarity)
# sqlite3 is part of Python stdlib — no extra install needed

# V2: CI/CD SARIF generation (pure Python; no extra deps)
# json, textwrap, pathlib — all stdlib
